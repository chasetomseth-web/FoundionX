import React from 'react';
import AppLayout from '@/components/AppLayout';
import StorefrontPageContent from './components/StorefrontPageContent';

export default function StorefrontPage() {
  return (
    <AppLayout>
      <StorefrontPageContent />
    </AppLayout>
  );
}
