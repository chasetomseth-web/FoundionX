/* eslint-disable */
// This wrapper intentionally does NOT require ts-node.
// It relies on the TypeScript file being executed via a loader set in package.json.
// If you see an error here, ts-node/loader is missing in the environment.

console.log('[seed-demo] starting (runner)');
require('./seed-demo.ts');


